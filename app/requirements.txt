Installations done after creating a virtual environment:

pip install "fastapi[standard]"
pip install sqlalchemy
pip install pyjwt
pip install "pwdlib[argon2]"
pip install pydantic-settings
pip install aiosqlite
pip instal greenlet
pip install trafilatura
pip install httpx
pip install celery
pip install redis
pip install nh3
pip install asyncpg
pip install alembic

System installations:

brew install redis
brew install postgresql@16
